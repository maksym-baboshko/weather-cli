export const TOKEN_DICTIONARY = { token: "token", city: "city" };

export const PARAM_ERR_MSG = {
  token: "The API key is not specified. Set it using the -t [API_KEY] command",
  city: "The city is not specified. Set it using the -c [CITY_NAME] command",
};

export const ONBOARDING_MSG = `To use the utility, you need to create a free API key on openweathermap.org\n And then add it with the commands below, as well as add a city`;
